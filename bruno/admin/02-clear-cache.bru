meta {
  name: 02 Clear Cache
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/clear-cache
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminAccessToken}}
}

tests {
  test("status 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
