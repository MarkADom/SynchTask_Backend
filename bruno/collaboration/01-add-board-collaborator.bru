meta {
  name: 01 Add Board Collaborator
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/boards/{{boardId}}/collaborators
  body: json
  auth: bearer
}

auth:bearer {
  token: {{userAccessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  "userEmail": "{{demoUserEmail}}"
}

tests {
  test("status 200 or 201", function () {
    expect([200,201]).to.include(res.getStatus());
  });
}
