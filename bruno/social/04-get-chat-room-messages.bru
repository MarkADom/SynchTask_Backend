meta {
  name: 04 Get Chat Room Messages
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/chat/rooms/{{chatRoomId}}/messages
  auth: bearer
}

auth:bearer {
  token: {{userAccessToken}}
}

tests {
  test("status 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
