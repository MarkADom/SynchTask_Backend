meta {
  name: Auth - POST /auth/refresh without refreshToken
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/auth/refresh
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

tests {
  test("status is 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
