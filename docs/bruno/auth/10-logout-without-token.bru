meta {
  name: Auth - DELETE /auth/logout without token
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: none
}

tests {
  test("status is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
