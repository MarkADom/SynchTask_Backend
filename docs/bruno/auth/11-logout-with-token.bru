meta {
  name: Auth - DELETE /auth/logout with token
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
