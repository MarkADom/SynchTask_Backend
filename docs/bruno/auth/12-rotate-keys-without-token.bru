meta {
  name: Auth - POST /auth/rotate-keys without token
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/auth/rotate-keys
  body: none
  auth: none
}

tests {
  test("status is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
