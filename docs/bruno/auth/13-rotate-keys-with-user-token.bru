meta {
  name: Auth - POST /auth/rotate-keys with non-admin token
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/auth/rotate-keys
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("status is 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
