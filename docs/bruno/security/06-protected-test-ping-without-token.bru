meta {
  name: Protected - GET /test/ping without token
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/test/ping
  body: none
  auth: none
}

tests {
  test("status is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
