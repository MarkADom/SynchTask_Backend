meta {
  name: Protected - GET /boards without token
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/boards
  body: none
  auth: none
}

tests {
  test("status is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
