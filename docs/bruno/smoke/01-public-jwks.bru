meta {
  name: Public - GET /jwks
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/jwks
  body: none
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("response contains keys array", function () {
    const json = res.getBody({ parse: true });
    expect(json.keys).to.exist;
  });
}
